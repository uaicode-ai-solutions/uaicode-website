
# Remover badge "Start Your Journey" da section ContactUs

## Mudanca

### `src/components/ContactUs.tsx`
- Remover o bloco do badge "Start Your Journey" (linhas 113-116), que contem o icone `Send` e o texto "Start Your Journey" dentro de um `div` com classes `inline-flex`.
- Remover o import do icone `Send` caso nao seja usado em outro lugar do componente.

Nenhuma outra alteracao sera feita. O titulo "Ready to Build Something Great?" e o subtitulo permanecem.
